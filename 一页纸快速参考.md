# 🚀 一页纸快速参考

## 准备（只需一次）

```bash
pip install -r requirements.txt    # 安装依赖
cd cleaner && python download_nltk.py    # ⚠️ 必须：下载 NLTK
redis-server    # 启动 Redis
```

## 启动（4 个终端，按顺序）

**Windows (双击 bat 文件)**:
```
1. cleaner\start_cleaner.bat
2. processer\start_processor.bat
3. scraper\run.bat → 选择 2
4. cd visualization/backend && python run.py
```

**命令行：**:
```bash
# 终端1: Cleaner（监听等待）
cd cleaner && start_cleaner

# 终端2: Processor（监听等待）
cd processer/Analysis && start_processor

# 终端3: Scraper（触发流程）
cd scraper && run

# 终端4: Backend（API服务）
cd visualization/backend && python run.py

# 终端5: Frontend
cd visualization/frontend && npm run dev
```

## 访问
- 前端: http://localhost:3000

## 停止

每个终端按 `Ctrl+C`

## 启动顺序说明

⚠️ **重要**：必须按 Cleaner → Processor → Scraper 顺序启动
- Cleaner、Processor 启动后在等待通知（正常）
- Scraper 启动后触发整个流程

## 常见问题

| 问题 | 解决 |
|------|------|
| Cleaner 报错 NLTK | `cd cleaner && python download_nltk.py` |
| Redis 连接失败 | `redis-server` |
| 模块没反应 | 正常！等待 Scraper 完成爬取（1-5分钟） |
| 重新开始 | `redis-cli flushall` |

📖 详细文档：[快速启动指南.md](快速启动指南.md)
