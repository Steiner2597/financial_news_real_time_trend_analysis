# 🧪 快速测试指南

## 1️⃣ 测试依赖安装

```cmd
cd c:\Users\LYH\Desktop\financial_news_real_time_trend_analysis-master\scraper
python test_imports.py
```

**期望输出：**
```
✅ 导入成功!
✅ utils.logger 可用
✅ utils.redis_client 可用
✅ RedditStreamCrawler 导入成功!
✨ 所有导入测试通过!
```

---

## 2️⃣ 测试配置

```cmd
python validate_config.py
```

确保 `reddit.stream_enabled: true`

---

## 3️⃣ 启动测试（推荐）

```cmd
run.bat
→ 选择 3 (循环模式)
```

**观察输出：**

1. 初始化阶段：
```
✓ Reddit 爬虫初始化成功
✓ Reddit 实时流式爬虫初始化成功
  → 将在循环模式下自动启动
```

2. 启动阶段：
```
🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥
🔴 启动 Reddit 实时流式监听（后台运行）
🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥🔥
✅ 实时流式监听已在后台启动
```

3. 运行阶段：
```
Reddit批量:  帖子 50, 评论 150, 错误 0
Reddit实时:  帖子 12, 错误 0 🔴 运行中
```

4. 停止阶段（Ctrl+C）：
```
🛑 正在停止实时流式监听...
✅ 实时流式监听已停止
   实时捕获统计: 12 条帖子, 0 个错误
```

---

## 4️⃣ 检查数据

```cmd
run.bat
→ 选择 4 (查看 Redis 数据)
```

应该看到 `source: reddit_stream` 的数据

---

## ❌ 如果遇到问题

### 问题1：ModuleNotFoundError
```cmd
pip install -r requirements.txt
```

### 问题2：Redis 连接失败
```cmd
start_redis.bat
```

### 问题3：实时流未启动
检查 `config.yaml`：
```yaml
reddit:
  stream_enabled: true  # 改为 true
```

---

## ✅ 成功标志

- ✅ 看到 "🔴 启动 Reddit 实时流式监听"
- ✅ 统计中显示 "Reddit实时: ... 🔴 运行中"
- ✅ 定期看到 "✅ 实时捕获: r/xxx | ..." 
- ✅ Ctrl+C 后优雅退出
