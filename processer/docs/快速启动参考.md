# 快速启动参考

## 🚀 一键启动（推荐）

```cmd
start_all_event_driven.bat
```

启动顺序：Redis → Cleaner → Scraper → Processor

---

## 📋 分步启动

### 1️⃣ 启动 Redis
```cmd
cd scraper
start_redis.bat
```

### 2️⃣ 启动 Cleaner（事件驱动）
```cmd
cd cleaner
start_cleaner.bat
```
💡 会等待爬虫通知

### 3️⃣ 启动 Scraper（循环）
```cmd
cd scraper
python control_center.py --loop
```
💡 每5分钟爬取一次

### 4️⃣ 启动 Processor（持续）
```cmd
cd processer
start_processor.bat
```
💡 每60秒处理一次

---

## 🧪 测试通知

```cmd
# 1. 启动 Cleaner
cd cleaner
start_cleaner.bat

# 2. 发送测试通知（新窗口）
python test_notification.py
```

---

## ⚙️ 运行模式

### 事件驱动（默认）
```cmd
python run_cleaner.py
```

### 持续轮询
```cmd
python run_cleaner.py --mode continuous
```

### 单次执行
```cmd
run_once_clean.bat
```

---

## 🔍 监控

### 查看 Cleaner 日志
```cmd
type cleaner\logs\event_driven_cleaner.log
```

### 监控通知频道
```cmd
redis-cli
> SUBSCRIBE crawler_complete
```

---

## ⚠️ 重要提示

1. **启动顺序**：必须先启动 Cleaner，再启动 Scraper
2. **频道名称**：Scraper 和 Cleaner 的 `channel` 必须一致
3. **Redis 必须运行**：所有服务依赖 Redis

---

## 📊 当前配置

| 模块 | 运行频率 | 说明 |
|------|---------|------|
| Scraper | 5分钟 | 自动爬取 |
| Cleaner | 按需 | 收到通知后执行 |
| Processor | 60秒 | 定期处理 |

---

## 🆘 常见问题

### 通知显示 0 个订阅者
→ Cleaner 未启动，先启动 Cleaner

### Cleaner 没收到通知
→ 检查频道名称是否一致

### 想禁用通知功能
→ 配置文件设置 `notification.enabled: false`

---

## 📁 配置文件

- `scraper/config.yaml` - Scraper 配置
- `cleaner/config_processing.yaml` - Cleaner 配置
- `processer/Analysis/config.py` - Processor 配置

---

## 📚 详细文档

- `爬虫清洗器事件驱动集成说明.md` - 完整使用说明
- `爬虫清洗器集成完成总结.md` - 改进总结
